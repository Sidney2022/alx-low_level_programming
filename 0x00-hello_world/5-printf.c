#include <stdio.h>

/**
 * main - main function
 *
 * Description: program to print certain a string using the printf function
 *
 * Return: 0
 */
int main(void)
{
	char myStr[] = "with proper grammar, but the outcome is a piece of art,\n";

	puts(myStr);
	return (0);
}
